# Core Dependencies
Flask==3.1.2
python-dotenv==1.2.1
PyYAML==6.0.3
requests==2.32.5

# Data Processing
numpy==2.3.5
pandas==2.3.3
pytz==2025.2
tqdm==4.67.1
tzlocal==5.3.1
pdfplumber==0.11.9
beautifulsoup4==4.13.3

# Training scripts (scripts/train_legacy_models.py) additionally require: optuna

# Machine Learning
scikit-learn==1.7.2
scipy==1.16.3
xgboost==3.1.2
joblib==1.5.2

# PyTorch - Optional for public release (MLP/Ensemble predictors)
# Required for transformer training (Phase 1+ neural model, needs CUDA GPU)
# CPU version (MLP predictor only):
#   --extra-index-url https://download.pytorch.org/whl/cpu
#   torch==2.8.0+cpu
# GPU version (transformer training):
#   pip install torch --index-url https://download.pytorch.org/whl/cu124

# NBA API
nba_api==1.11.3

# openai - removed (generative path deprecated)

# Logging
python-json-logger==4.0.0

# Testing
pytest==9.0.1
pytest-cov==7.0.0

# Visualization
matplotlib==3.9.1
seaborn==0.13.2

# Removed unused dependencies:
# - wandb (experiment tracking - removed, not needed)
# - docker-pycreds (was a wandb dependency, not directly used)
# - pyan3 (code analysis tool, not needed for runtime)
# - Many transitive dependencies that pip will install automatically
aiohttp>=3.9.0  # collect_pbpstats.py
